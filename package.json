{
    "name": "waterjunction",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "build": "npm install --include=dev --prefix frontend && npm install --prefix backend && npm run build --prefix frontend",
      "start": "node backend/server.js"
    }
  }
  